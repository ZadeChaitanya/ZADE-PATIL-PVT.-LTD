<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Login</title>

  <!-- Bootstrap 5.3 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: #f4f6f9;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: Arial, sans-serif;
    }

    .login-card {
      background: #fff;
      padding: 35px 30px;
      border-radius: 12px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 420px;
    }

    .login-title {
      font-size: 26px;
      font-weight: 600;
      text-align: center;
      margin-bottom: 25px;
      color: #333;
    }

    .form-control {
      height: 48px;
      border-radius: 8px;
    }

    .btn-login {
      width: 100%;
      height: 48px;
      font-size: 16px;
      border-radius: 8px;
    }

    .signup-link {
      text-align: center;
      margin-top: 15px;
      font-size: 14px;
    }
  </style>
</head>

<body class="bg-secondary">

  <div class="bg-dark text-light login-card">
    <div class="login-title text-light">Admin Panel Login</div>

    <form action="/admin/verify_admin" method="post">

      <div class="mb-3">
        <label class="form-label text-light">Mobile Number</label>
        <input type="number" name="admin_mobile" class="form-control text-light bg-secondary" required />
      </div>

      <div class="mb-4">
        <label class="form-label text-light">Password</label>
        <input type="password" name="admin_password" class="form-control text-light bg-secondary" required />
      </div>

      <button class="btn btn-danger btn-login">Login</button>

      <!-- <div class="signup-link">
        <a href="/admin/create_account" class="text-warning">Don't have an account? <strong>Sign Up</strong></a>
      </div> -->

    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
